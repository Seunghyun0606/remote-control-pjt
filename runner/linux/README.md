# Linux Runner

R0 runs the Codex runner locally inside the Controller process on Lightsail.

A standalone Linux runner daemon is introduced together with the WebSocket transport in Phase R1 so Linux and Windows hosts use the same protocol.
